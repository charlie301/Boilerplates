var Child = React.createClass({
     render: function(){
          return (
               <div>
                    and this yah is the <b>{this.props.name}</b>
               </div>
          )
     }
});
